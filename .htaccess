Options -Indexes
RewriteEngine On

# Route /blog/{slug} to single-post.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^blog/([^/]+)/?$ single-post.php?slug=$1 [QSA,L]
